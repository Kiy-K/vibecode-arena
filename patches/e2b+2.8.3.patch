diff --git a/node_modules/e2b/dist/index.js b/node_modules/e2b/dist/index.js
index 053f369..3d37ff2 100644
--- a/node_modules/e2b/dist/index.js
+++ b/node_modules/e2b/dist/index.js
@@ -2695,10 +2695,15 @@ function getCallerDirectory(depth) {
   if (callSites.length === 0) {
     return void 0;
   }
-  const fileName = callSites[0].getFileName();
+  let fileName = callSites[0].getFileName();
   if (!fileName) {
     return void 0;
   }
+  // Handle file:// URLs from ESM modules
+  if (fileName.startsWith('file:')) {
+    const { fileURLToPath } = require('url');
+    fileName = fileURLToPath(fileName);
+  }
   return import_node_path.default.dirname(fileName);
 }
 function padOctal(mode) {
diff --git a/node_modules/e2b/dist/index.mjs b/node_modules/e2b/dist/index.mjs
index 994fe63..b625e7a 100644
--- a/node_modules/e2b/dist/index.mjs
+++ b/node_modules/e2b/dist/index.mjs
@@ -2660,10 +2660,15 @@ function getCallerDirectory(depth) {
   if (callSites.length === 0) {
     return void 0;
   }
-  const fileName = callSites[0].getFileName();
+  let fileName = callSites[0].getFileName();
   if (!fileName) {
     return void 0;
   }
+  // Handle file:// URLs from ESM modules
+  if (fileName.startsWith('file:')) {
+    const { fileURLToPath } = require('url');
+    fileName = fileURLToPath(fileName);
+  }
   return path.dirname(fileName);
 }
 function padOctal(mode) {
